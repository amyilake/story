<div class='row'>
  <% @posts.each do |post| %>
    <div class="span4">
        留言數<%= post.comments.count %> |
        按贊
      <div class="well">
        <%= link_to(post.title , post_path(post.id)) %> | 
        <%= post.author.name %> 
        <br/>
      </div>
    </div>
  <% end%>
</div>
